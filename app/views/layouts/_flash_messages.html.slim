- flash.each do |name, msg|
 - if msg.is_a?(String)
    div[class="alert alert-dismissible alert-#{name.to_s == 'notice' ? 'success' : 'error'} center-block text-center bg-danger"]
      = content_tag :div, msg, :id => "flash_#{name}"
  