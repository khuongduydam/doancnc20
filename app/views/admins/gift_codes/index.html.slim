.container[style="margin:-9% 0 0 20%;width:100%;"]
  .table-responsive
    table.table.table-bordered
      thead
        tr.info
          th
            .col-sm-2.col-md-2
              button.btn.btn-info.10 Gift Code 10%
            .col-sm-2.col-md-2
              button.btn.btn-info.20 Gift Code 20% 
            .col-sm-2.col-md-2
              button.btn.btn-info.30 Gift Code 30%
            .col-sm-2.col-md-2
              button.btn.btn-info.40 Gift Code 40%
            .col-sm-2.col-md-2
              button.btn.btn-info.50 Gift Code 50%
            .col-sm-2.col-md-2
              button.btn.btn-info.60 Gift Code 60%   
.container[style="margin-left:20%;width:100%;"]
  input.gift-code.form-control.center-block[style="border:1px solid #ccc;height:50px;width:50%"]

javascript:
  function makeCode(){
    var text = "";
    var possible = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    for( var i=0; i < number; i++ )
      text += possible.charAt(Math.floor(Math.random() * possible.length));
      $('input.gift-code').val(text).css('text-align','center')
  }  
  $('.10').on('click',function(){
    number = 50;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });
  $('.20').on('click',function(){
    number = 51;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });
  $('.30').on('click',function(){
    number = 52;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });
  $('.40').on('click',function(){
    number = 53;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });
  $('.50').on('click',function(){
    number = 54;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });
  $('.60').on('click',function(){
    number = 55;
    makeCode();
    $.ajax({
      type: "POST",
      beforeSend: function(xhr) {xhr.setRequestHeader('X-CSRF-Token', $('meta[name="csrf-token"]').attr('content'))},
      url: "/#{params[:locale]}/admins/gift_codes",
      dataType: 'script',
      data: {code: $('input.gift-code').val()}
    })
  });