.container-fluid
  .row
    .col-xs-3.col-md-3
      h3[style="color:blue;"] Please enter your details
      = simple_form_for @order do |f|
        = f.input :name
        = f.input :address
        = f.input :email
        = f.input :phone
        = f.input :note
        = f.select :pay_type, Order::PAYMENT_TYPES , prompt: 'Choose one'
        = f.submit 'Submit',class:' btn btn-success pull-right btn-sm'
    .col-xs-9.col-md-9
        - unless @cart.order_items.empty?
          .container-fluid
            h3.text-center[style="color:blue"] Your Cart
            .table-responsive
              table.table
                thead
                  tr.danger
                    th.text-center.col-xs-4.col-md-4 Product
                    th.text-center.col-xs-2.col-md-2 Quantity
                    th.text-center.col-xs-2.col-md-2 Price
                    th.text-center.col-xs-2.col-md-2 Money
                tbody
                  - @cart.order_items.each do |item|
                    tr.success
                      td.text-center.col-xs-4.col-md-4 
                        = item.product.name
                      td.text-center.col-xs-2.col-md-2 
                        = item.quantity
                      td.text-center.col-xs-2.col-md-2 
                        = number_with_delimiter(item.product.price.to_i,delimiter:'.')
                        | 
                      td.text-center.col-xs-2.col-md-2 
                        = number_with_delimiter(item.total_price.to_i,delimiter:'.')
                        |
              .pull-left
                h4[style="color:red"] 
                  | Total: 
                  = number_with_delimiter(@cart.total_price.to_i,delimiter:'.')
              .pull-right
                = link_to 'Empty Cart',@cart, method: :delete, data: {confirm: 'Are you sure?'},class:'btn btn-danger btn-sm'
